include('scripts/WAKLIB_Server.js'); //Utility library - open source (www.waklib.com)IN = {}; //Set up our application namespace on the server sideinclude('scripts/language/en.js'); //english is default language - changes on login if needed//Common functionsIN.getCurrentAccount = function(){	//returns the account entity of the current session	return ds.Account.find('ID = :1', currentSession().storage.accountID);}IN.getCurrentUser = function(){	//returns the user entity of the current user	return ds.User.find('username = :1', currentUser().name);}