//Methods(Entity Methods, Collection Methods, Datastore Class Methods), Events (Attribute Events, Datastore Class Events, Calculated Attributes)model.Registration = {};//*** METHODS//model.XXXX.entityMethods = {};//model.XXXX.collectionMethods = {};model.Registration.methods = {};model.Registration.methods.newRegistration = function(regObject){	var status = ''; //holds the status of each save		ds.startTransaction();	try{		var theCompany = new ds.Company({name: companyName});		theCompany.save();		var adminPerson = new ds.Person({firstName: adminFirstName, lastName: adminLastName});		adminUserPerson.save();		var adminUser = new ds.User({username: adminUsername, password: adminPassword, account: theAccount, person: adminPerson});		adminUser.save();		var theAccount = new ds.Account({name: accountName});		theAccount.company = theCompany;		theAccount.save();				loginByPassword(userName);					return true;	} catch (err) {		//deal with error	}	ds.commit();}model.Registration.methods.newRegistration.scope = 'public';//*** EVENTS//model.XXXX.attributeName = {};//model.XXXX.attributeName.events = {};//model.XXXX.events = {};//Calcuated attributes